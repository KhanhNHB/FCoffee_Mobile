<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fcoffee">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".modules.billInfo.activity.BillInfoActivity"></activity>
        <activity android:name=".modules.account.activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".modules.table.activity.TableActivity">

        </activity>
        <activity android:name=".modules.table.activity.TableDetailActivity">
        </activity>
        <activity android:name=".modules.dink.activity.DrinkActivity"></activity>
    </application>

</manifest>